import "../../globals.css";

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function CartRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      <h1>Cart Root Layout</h1>
      {children}
    </div>
  );
}
